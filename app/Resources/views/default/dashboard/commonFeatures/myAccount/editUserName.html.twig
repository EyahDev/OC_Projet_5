{% extends 'base.html.twig' %}

{% block body %}
    <div class="well">
        {{ form_start(updateNameForm, {'attr': {'class': 'form-horizontal'}}) }}

        {# Les erreurs générales du formulaire. #}
        {{ form_errors(updateNameForm) }}

        {# Génération du label + error + widget pour un champ. #}
        <div class="form-group">
            {{ form_row(updateNameForm.name) }}
            {{ form_widget(updateNameForm.save, {'label': 'Modifier', 'attr': {'class': 'btn btn-primary'}}) }}
        </div>
        {# Génération automatique des champs pas encore écrits.
           Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
           et tous les champs cachés (type « hidden »). #}
        {{ form_rest(updateNameForm) }}
        {# Fermeture de la balise <form> du formulaire HTML #}
        {{ form_end(updateNameForm) }}
    </div>
{% endblock %}