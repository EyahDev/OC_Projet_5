{% extends 'base.html.twig' %}

{% block body %}
    <div class="well">
        {{ form_start(addLocationForm, {'attr': {'class': 'form-horizontal'}}) }}

        {# Les erreurs générales du formulaire. #}
        {{ form_errors(addLocationForm) }}

        {# Génération du label + error + widget pour un champ. #}
        <div class="form-group">
            {{ form_row(addLocationForm.location) }}
            {{ form_widget(addLocationForm.save, {'label': 'Modifier', 'attr': {'class': 'btn btn-primary'}}) }}
        </div>
        {# Génération automatique des champs pas encore écrits.
           Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
           et tous les champs cachés (type « hidden »). #}
        {{ form_rest(addLocationForm) }}
        {# Fermeture de la balise <form> du formulaire HTML #}
        {{ form_end(addLocationForm) }}
    </div>
{% endblock %}