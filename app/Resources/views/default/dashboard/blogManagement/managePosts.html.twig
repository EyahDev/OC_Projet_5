{% if postsList|length == 0 %}
    Aucun articles n'a été publié pour l'instant
{% endif %}

{% for post in postsList %}
    titre : {{ post.title }} -- posté le : {{ post.publishedDate|date("d/m/Y à h:i", "Europe/Paris") }} -- extrait : {{ post.content|slice(0, 15) }}
    -- catégorie : {{ post.category.name }} -- commentaires : {{ post.comments|length }} --
    <a href="{{path('edit_post', {'slug' : post.slug }) }}" ><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
    /
    <a href="{{path('advice_delete_post', {'slug' : post.slug }) }}"><i class="fa fa-times" aria-hidden="true"></i><br/></a><br/>
{% endfor %}